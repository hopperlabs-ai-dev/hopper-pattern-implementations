import http from "node:http";
import { exampleRegistry } from "../../../packages/orchestration-runtime/src/index.mjs";
const port = Number(process.env.HOPPER_ORCHESTRATION_PORT || 47623); const allowed = ["numbers.filter", "numbers.summarize"];
const server = http.createServer(async (request, response) => { try { if (request.method === "GET" && request.url === "/health") { response.writeHead(200, { "content-type": "application/json" }).end(JSON.stringify({ ok: true })); return; } if (request.method !== "POST" || request.url !== "/v1/plan") { response.writeHead(404).end(); return; } const chunks=[]; let bytes=0; for await(const chunk of request){bytes+=chunk.length;if(bytes>16_384){response.writeHead(413).end();return;}chunks.push(chunk);} const result=await exampleRegistry.executePlan(JSON.parse(Buffer.concat(chunks).toString("utf8")),allowed); response.writeHead(200,{"content-type":"application/json","cache-control":"no-store"}).end(JSON.stringify(result)); } catch(error){response.writeHead(400,{"content-type":"application/json"}).end(JSON.stringify({error:error.message}));} });
server.listen(port,"127.0.0.1",()=>console.log(`Orchestration reference http://127.0.0.1:${port}`));
